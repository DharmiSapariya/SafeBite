pbpaste > README.md        # after copying the new README with Cmd+A, Cmd+C
head -5 README.md          # should show the 🥗 title
git add README.md
git commit -m "docs: rewrite README with flows, diagrams, API map and status"
git push origin main